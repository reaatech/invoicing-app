import { type LucideIcon } from 'lucide-react';
export declare const getStatusCssColor: (status: string) => string;
export declare function getStatusIcon(status: string): LucideIcon;
export declare function canEditInvoice(status: string): boolean;
export declare function canDeleteInvoice(status: string): boolean;
